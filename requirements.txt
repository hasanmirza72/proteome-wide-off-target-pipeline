# Python dependencies for the proteome-wide off-target pipeline
# Exact versions from the analysis environment (Berzelius cluster).
# Install with:  pip install -r requirements.txt

numpy==2.2.6
pandas==2.3.3
biopython==1.87
chembl-webresource-client==0.10.9
requests==2.34.2
requests-cache==1.3.2

# PyMOL 3.1.0 is used for structural superposition; it is typically installed
# separately (conda-forge or the Schrodinger distribution), not via pip:
#   conda install -c conda-forge pymol-open-source=3.1.0
#
# External command-line tools (install separately, must be on PATH):
#   FoldDisco  (commit de68e15, build from source: https://github.com/steineggerlab/folddisco)
#   P2Rank     2.4.2
#   BLAST+     2.17.0
#   Foldseek   10 (10.941cd33)
